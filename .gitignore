# ESP-IDF Build-Ausgaben (jedes Projekt hat sein eigenes build/)
build/
**/build/
sdkconfig
sdkconfig.old
**/sdkconfig
**/sdkconfig.old
*.o
*.elf
*.bin
*.map

# Komponenten aus der Component Registry (werden aus idf_component.yml generiert)
managed_components/
**/dependencies.lock

# Generierte Versions-/Build-Dateien (werden bei jedem Build neu erzeugt)
**/include/version.h
.build_number
.last_version

# IDE / Editor
.vscode/
.idea/
*.swp

# OS
Thumbs.db
.DS_Store
